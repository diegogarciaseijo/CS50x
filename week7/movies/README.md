# Movies

SQL queries for the CS50 SQL Movies problem set.

The queries were tested using the `movies.db` database provided by CS50, which contains data from IMDb. The database itself is not included in this repository because of its size.
